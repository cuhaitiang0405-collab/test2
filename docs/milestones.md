# MDT 系统 — 里程碑开发计划（含检查点）

> 协作方式：**按里程碑分段开发 → 每段自测（字段更新 / 后端对接）→ 预览给你确认 → 你许可后才进下一里程碑**。
> 需求有疑问先问、不蛮干。UI 走**医疗蓝**，设计系统由 Impeccable 生成。

---

## 已锁定的地基决策（不推翻既有骨架）

| 项 | 决策 |
|---|---|
| 后端语言 | **Spring Boot (Java)**，DDD（XxxService/XxxRepository） |
| 前端框架 | **Vue 3 + TS（Composition API）** |
| 外部对接 | **Mock 优先**：dcm4che 模拟 PACS + Mock HIS/EMR 中间表，真实环境后续独立接入 |
| 开发库 | **Postgres**（生产复用已写好的 Oracle/SQL Server 兼容 DDL） |
| UI 基调 | **Impeccable 生成医疗蓝设计系统**（主色蓝，浅色专业风） |
| M1 范围 | **脚手架 + 合规底座**（先地基，后续叠加） |

---

## 里程碑总览（M1→M7，每段独立检查点）

> 状态图例：**✅ 已落地**（M1–M3 已完成并经预览）｜**⏳ 规划中**｜**🆕 新增**（来自需求缺口，见下「缺口 Backlog」）

| 里程碑 | 目标 | 关键交付 | 自测重点 | 预览物 | 状态 |
|---|---|---|---|---|---|
| **M1** | 脚手架+合规底座 | 六域 Spring Boot 模块、Vue3 脚手架、Postgres、API 网关、gRPC、TraceId/审计/脱敏、登录(医疗蓝) | 字段更新落库回读；网关→服务 gRPC 200；日志脱敏生效 | 登录页 + 空壳工作台（可访问 URL） | ✅ |
| **M2** | 数据接入层 | DICOM SCP 接收 + SCU C-FIND/C-MOVE(模拟PACS)、HIS/EMR Mock 适配(中间表/视图)、PatientVisitUID 统一索引 | 模拟推送/拉取影像入库；V_PATIENT_STUDIES 视图可查；临床字段正确 | 数据接入看板（接入记录列表） | ✅ |
| **M3** | 影像引擎+Web阅片 | WADO 取帧、WebGL 窗宽窗位/MPR/测量/序列对比(Vue,医疗蓝)、Worker 离主线程 | 零下载加载序列；调窗/测量/MPR 正确；主线程不阻塞(帧率) | 阅片工作站界面 | ✅ |
| **M4** | 会诊状态机闭环 | 状态机(申请→通知→确认→进行中→总结)、短信异步队列(mock)、前端申请/列表/详情/总结 | 非法迁移拦截；全员确认门控；结论字段更新回读 | 会诊流程页面 | ⏳ |
| **M5** | 协同通讯层 | WebRTC SFU、电子白板、标注序列化、桌面共享 | 两浏览器入房；音视频通；白板标注同步 | 会诊协同房间 | ⏳ |
| **M6** | 权限租户+部署 | 字段级 RBAC、多租户隔离、负载均衡策略接入云影像 SPI、docker-compose 部署 | 越权字段拦截；跨租户不可见；负载策略切换；部署可访问 | 完整系统 + 部署文档 | ⏳ |
| **M7** | 扩展功能（全新需求） | 个性化模板引擎、会诊录制服务、手术室 DSA/腔镜影像接入、四级手术会诊分级 | 模板可定制并复用；会诊过程可录制回放；手术级模态接入；分级会诊门控 | 模板+录制+手术级会诊演示 | 🆕 |

### 缺口 Backlog（来自 `docs/requirements.md` 的 GAP-1~11）

| 缺口 | 描述 | 落入里程碑 | 类型 |
|---|---|---|---|
| GAP-1 | 个性化模板（检查/讨论/病历模板定制） | **M7** | 全新功能 |
| GAP-2 | 会诊录制 / 视频录制（全过程无容量限制） | **M7** | 全新功能 |
| GAP-3 | 手术室影像接入（DSA/腔镜手术级影像） | **M7** | 全新功能 |
| GAP-4 | 四级手术多学科会诊（会诊分级） | **M7** | 全新功能 |
| GAP-5 | 负载均衡「随机 RANDOM」策略 | **M6** | 一行枚举补丁 |
| GAP-6 | 高级图像处理：悬挂布局/电影播放/JPEG2000/真彩色/反相/预设窗 | **M3 迭代** | 细化增强 |
| GAP-7 | 文字注释(text annotation) + 跨机构影像发布 | **M3** | 细化增强 |
| GAP-8 | 自动/手动关联影像逻辑 | **M2/M3 衔接** | 细化增强 |
| GAP-9 | 诊疗数据细类：入院病历/长期医嘱/临时医嘱枚举 | **M2** | 细化增强 |
| GAP-10 | 医疗单位 CRUD 管理 UI + 住院按科室/病区分组查询 + 门诊按姓名/门诊号查询 | **M6** | 细化增强 |
| GAP-11 | 区域专家会诊分级 + 专家所属机构标记 | **M4/M6** | 细化增强 |

---

## 每里程碑统一自测清单（"字段能否更新 / 后端能否对接"）

1. **字段更新**：经前端/API 改一条记录 → 落库 → 回读一致（断言相等）。
2. **后端对接**：网关 REST → 域内 gRPC 调用返回预期（含 TraceId 透传断言）。
3. **合规**：写一条含姓名/身份证的测试日志 → 断言输出仅剩 PatientID+检查号掩码。
4. **预览**：启动 dev 环境，给出可访问 URL / 截图，标注本里程碑完成的功能。

---

## 假设项（如有误请纠正，否则按此推进）

- **认证**：M1 先用简单 **JWT（用户名/密码 + mock 用户表）**，后续可接医院统一认证（OAuth2）。
- **域间通信**：六域间 **gRPC**，对外统一 **REST via API 网关**（与 `proto/mdt.proto` 对齐）。
- **负载均衡**：作为 ⑥ 扩展层策略，在 **M6** 接入云影像 SPI；M1 不含。
- **真实系统**：全程 **Mock**；真实 PACS/HIS 接入作为 M2 之后的独立任务，不阻塞里程碑。
- **Impeccable**：在 **M1 的 UI（登录+空壳）** 即启用，生成医疗蓝设计 token（主色/间距/组件），后续里程碑复用。

---

## 当前进度

- [x] **M1 脚手架与合规底座** ✅ 完成（登录+合规底座+医疗蓝 UI，见 `m1-completion.md`）
- [x] **M2 数据接入层** ✅ 完成（DICOM SCP/SCU 模拟 + HIS/EMR Mock + 统一索引，见 `m2-completion.md`）
- [x] **M3 影像引擎与 Web 阅片** ✅ 完成（WebGL2 MPR/窗宽窗位/测量，见 `m3-completion.md`）
- [x] **M4 会诊状态机闭环** ✅ 完成（申请→通知→确认→进行中→总结，Mock SMS 异步队列）
- [x] **M5 协同通讯层** ✅ 完成（WebRTC Mesh 信令 + 电子白板 + 标注序列化 + 桌面共享，见 m5-preview/）
- [ ] M6 权限租户与部署
- [ ] **M7 扩展功能（全新需求）** ← 由需求缺口 GAP-1~4 新建

> 下一步：你许可后我开始 **M6**（权限租户/部署/负载均衡/云影像 SPI），M5 已自测+预览通过。
> 需求追溯见 `docs/requirements.md`（docx 26 模块 → 域/里程碑/记录状态/实现状态）。
