server:
  port: 8086

lb:
  strategy: ROUND_ROBIN          # RANDOM / WEIGHTED_ROUND_ROBIN / LEAST_CONNECTIONS / SOURCE_HASH
  backends: http://localhost:8083 # 生产追加: http://cloud-1:8083,http://cloud-2:8083

management:
  endpoints:
    web:
      exposure:
        include: health,info
